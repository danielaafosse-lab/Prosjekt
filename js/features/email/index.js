/**
 * email — public surface of the email feature.
 *
 * Other parts of the app import { emailService } from '.../features/email/index.js'.
 * Internal files within this feature are implementation detail.
 */
export { emailService } from './services/emailService.js';
